pipeline {
    agent any

    stages {
        stage("Git Checkout") {
            steps {
                script {
                    git branch: 'main', credentialsId: 'khushi_cred', url: 'https://github.com/OT-MyGurukulam/employee-api.git'
                }
            }
        }
        stage('Static Code Analysis') {
            environment {
                PATH = "/opt/sonar-scanner/bin:$PATH"
            }
            steps {
                sh '''
                sonarscanner \
                    -Dsonar.projectKey=golang-SCA \
                    -Dsonar.sources=. \
                    -Dsonar.host.url=http://13.201.35.61:9000/
                    -Dsonar.login=squ_11b031411f0d15601ed4b4e075e62721c99ee078
                '''
            }
        }
    }

    post {
        always {
            cleanWs cleanWhenSuccess: false
        }
        success {
            echo 'Build Successfully !'
        }
        failure {
            echo 'Build Failed !'
        }
    }
}
