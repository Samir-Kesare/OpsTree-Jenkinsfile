pipeline {
    agent any
    environment {
        ANSIBLE_PRIVATE_KEY=credentials('vd-key')
    }
    stages {
        stage('GIT Checkout') {
            steps {
                checkout scmGit(branches: [[name: 'Vidhi/Kibana-dashboard-role']], extensions: [], userRemoteConfigs: [[credentialsId: 'token-github', url: 'https://github.com/CodeOps-Hub/Ansible.git']])
            }
        }
        stage('ansible inventory') {
            steps {
                sh 'ansible --version'
                sh 'ansible-inventory --graph'
            }
        }
        stage('Run Ansible') {
            steps {
                sh 'ansible --version'
                sh 'ansible-playbook --private-key=$ANSIBLE_PRIVATE_KEY kibana_dashboard.yml'
            }
        }
    }
}
