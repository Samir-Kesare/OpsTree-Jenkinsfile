s@Library('p7-shared-library') _ 
def ansible_CD = new org.avengers.template.ansible_CD.ansibleCD()
pipeline {
    agent any
    stages {
        stage ('Playbook CD'){
            steps{
                script{
                    def url = 'https://github.com/CodeOps-Hub/Ansible.git'
                    def creds = 'shikha'
                    def branch = 'main'
                    def gitleaksVersion = '8.18.2'
                    def path = '--private-key /var/lib/jenkins/prometheus.pem /var/lib/jenkins/workspace/elasticsearch/roles/elasticsearc/playbook.yml'
                    // Shared Libray for CD
                    ansible_CD.call(url, creds, branch, gitleaksVersion, path)
                    // ansible_CD.call(url, creds, branch, path)
                }
            }
             
        }
    }
}
